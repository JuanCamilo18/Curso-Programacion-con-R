

# ¿La función break en un bucle for() detiene la operación y prosigue con la siguiente iteración?

# F

## 2 
# Qué proceso realiza la siguiente sintaxis:
  
  for(i in 1:10) {
    if(i %% 2 == 0)next 
    print(i)
  }
 
#Calcula el residuo de la división y si esta es igual a cero pasa a la siguiente iteración.

## 3
# ¿La condicional if sólo evalúa el primer elemento si no está contenida por un for?
# V

## 4
# Si defino el siguiente vector:
  
x<-1:20
  
# Y deseo incluirlo en una condicional if de la siguiente manera. 
if (x<10){
  print("Vaya que está mal")
}
  
# Me sale un mensaje de advertencia ¿a que se debe este problema?

# Porque la condicional if sólo considera el primer elemento.



